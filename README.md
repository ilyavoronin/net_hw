jar files could be found in build/libs  
### Server  
build: `./gradlew buildServer`  
launch: `java -jar server.jar <port> <maxThreads>`  

### Client
build: `./gradlew buildClient`  
launch: `java -jar server.jar <host> <port>`  
